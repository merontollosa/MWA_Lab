import { MyvisibliltyDirective } from './myvisiblilty.directive';

describe('MyvisibliltyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyvisibliltyDirective();
    expect(directive).toBeTruthy();
  });
});
